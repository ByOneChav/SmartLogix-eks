server:
  port: 8081

spring:
  application:
    name: msvc-auth
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

application:
  security:
    jwt:
      secret-key: ${JWT_SECRET:9f8d7a6s5d4f3g2h1j0k9l8m7n6b5v4c3x2z1q}
      expiration: 86400000
