apiVersion: v1
kind: Service
metadata:
  name: msvc-config               # los demás servicios se conectan a http://msvc-config:8888
spec:
  selector:
    app: msvc-config
  ports:
    - port: 8888
      targetPort: 8888
  type: ClusterIP                  # solo accesible dentro del clúster